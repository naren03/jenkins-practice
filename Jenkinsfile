pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Check out source code from a version control system (e.g., Git)
                echo "checking out..."
            }
        }

        stage('Build') {
            steps {
                // Build your project (e.g., compile code)
                echo "uilding out..."
            }
        }

        stage('Test') {
            steps {
                // Run tests on your project
                echo "testing out..."
            }
        }

        stage('Deploy') {
            steps {
                // Deploy your application to a specific environment
                echo "deploying out..."
            }
        }
    }
}
